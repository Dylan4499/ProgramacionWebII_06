@model IEnumerable<agenciaDeViajesV2.Models.HotelClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.hotelName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.addressAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.stars)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imageHotel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.createdAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.updatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.states)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.hotelName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.addressAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.stars)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imageHotel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.createdAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.updatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.states)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PK_hotel">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PK_hotel">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PK_hotel">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
